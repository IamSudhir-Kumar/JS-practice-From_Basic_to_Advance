/* Create a function that checks if a given string is
 the same when read forwards and backward (e.g., "racecar" or "level").
  You can remove spaces and convert characters to lowercase for a simple 
  solution.*/
